import HeroContainer from '../../Reusable/HeroContainer/HeroContainer';
import Heading1 from '../../Reusable/Heading1/Heading1';

const ContactUsHero = () => {
    return (
        <HeroContainer classNames='py-[64px]'>
            <Heading1 classNames='text-white text-center'>Contact Us</Heading1>
        </HeroContainer>
    );
};

export default ContactUsHero;