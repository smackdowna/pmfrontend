import { ICONS } from "../../../assets";

const Navbar = () => {
    return (
        <div>
            <img src={ICONS.hamburgerMenu} alt="" className="" />
        </div>
    );
};

export default Navbar;